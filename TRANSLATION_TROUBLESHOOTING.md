ct
s correpath i
- The key on existslatiransd
- The tace is loademespnae 
- Thntify if:help ide will `

This);
``The')lcomeToogin.we:linists('adm, i18n.ex key:'.log('Hassoleon));
comeToThe'gin.welc, t('loranslation:'sole.log('T
conons.ns);ptii18n.oespaces:', ed namg('Loadloe.
consol;anguage)', i18n.luage:Current lang('
console.log;
('admin')tionsla useTrant, i18n } =onst { ript
cesc

```typ debug:ent to compon to yourthis

Add Testingssue

##  ie the isolatrk** tons wotionslaother tra if 
5. **Check is workingthe systemrify ey** to ve kesting a new t addTryes/`
4. **/localend/publicnt`fro**: tioncorrect locan les are iCheck if fi
3. **n filestioon translaissions** permle Verify firors
2. **18n er for ionsole**owser c **Check br?

1. Not Workingtill``

## S
`ldrun buim ion
npr product# Or fodev

run st
npm s/.vite diodule_m
rm -rf noderontendestart
cd fand ring yth Clear ever
```bash
#ds
mmanck Fix Co
## Qui```
..
  }
}

    .تنس",اليتعلق بوكل ما حجوزات العب ودارة الملالخاص بك لإقوة ا "مركز الwerhouse":adminPo  "ة",
  إدار"عالم الniverse": inUadm  "في",
  باً بك he": "مرح"welcomeToT: {
    "
  "login
```json
{son`)`ar/admin.jrabic (``

### A}
`}
  ...
    
nis",rything tens, and evebooking courts, for managingwerhouse : "Your powerhouse""adminPo rse",
   iven Un"Admi": verse"adminUni
    to the","Welcome eToThe": omwelc  "{
  login": n
{
  ")
```jso.json`/adminnglish (`en

### E Valuesanslationnt Trre

## Cur namespace)g 'admin'` (missineToThen.welcomg: `logiWron- ❌ )
owercase 't'omeTothe` (llcogin.wen.lg: `admi`
- ❌ WronToThein.welcomein.log `admorrect: C
- ✅t key path:fy the exacpos
VeriTy Check for # 7.orrect

## is ce contentrify th ve ton themClick oson`
5. ar/admin.jd `/locales/ann` /admin.jso`/locales/enuests to k for reqoo. Le page
4Refresh thk tab
3. twor Ne. Go to
22) (F1er DevTools Open brows:

1.ccessible are aon filesranslatick that the Served
Ciles Areslation Fy Tran# 6. Verif``

##
  );
}
`/h1>e')}<.welcomeToThin('log<h1>{t (
    
  returnce
   namespain''adm specify '); // Musttion('adminuseTranslat { t } = {
  consin() ogdminLfunction At';

-i18nexreaction } from 'anslatuseTrmport { Login.tsx
i
// Admincript
```typesace:
namesphe correct  is using t componenty theVerifsage
onent Ueck Comp. Ch

### 5
``` },
  });',
   {ns}}.jsonng}}/{s/{{localePath: '/lad lond: {
     
    backe,mon'S: 'com defaultNis here
   re 'admin' ke suMa'], // ', 'admin: ['common ns
   ',enackLng: 'lb    fal
  .init({
8next)eactI1tR.use(initector)
  nguageDese(La
  .u)ndckese(Ba.un
  milar
i1818n.ts or sitend/src/ironscript
// f

```type8n config:i1r youed in loadamespace is n nthe admi that erifyon
Vgurati8n ConfiCheck i1`

### 4. un build
``
npm rebuild
# R
distm -rf ts
rd artifacuilClean b# end

ont fr``bash
cdld:

`duction buig a prod
If runninontenld the Frui### 3. Rebev
```

n d
npm rucd frontend server
art dev
# Rests/.vite
odule_montend/noderf frache
rm - Vite cear
# Cl+C)
er (Ctrlv serve deop thh
# St
```base:
lopment modng in devehe
If runnierver Cac Sar Vite Dev2. Cle### x)

P` (Firefoft+hi+SCtrlor `e) +N` (Chromhift*
- `Ctrl+Ste mode:*gnito/Privar use Inco)

**OfreshF5` (hard re`Ctrl+ page with theh esefrta"
4. R"Clear da3. Click les"
 fi images andedlect "Cach2. Sec)
ete` (Mat+Del`Cmd+Shifor ndows)  (Wiete`ft+DelCtrl+Shi. Press `
1rome/Edge:****Chn files.

lationg old transht be cachiser migrowche
The bBrowser Ca# 1. Clear 

##lutions
## Soly valid
syntacticales are JSON filon`
✅ Both n.jses/ar/admialc/locbliontend/pu in `frtslation exis
✅ Transjson`es/en/admin.ic/localpubltend/`fronn exists in  Translation
✅rificatio.

## Vefilesthe JSON sts in but it exi, ingwing as missshotion key  translaeToThe`in.welcomlog`admin. Issue
e

##uid Gbleshootingon Trou Translati#